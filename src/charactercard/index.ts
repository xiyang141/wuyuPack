import { lib, game, ui, get, ai, _status } from "noname";

let initCharacterCard = () => {
	lib.init.css(lib.assetURL + "extension/无语包/src/charactercard", "index");
};

export { initCharacterCard };
